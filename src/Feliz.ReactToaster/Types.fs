namespace Feliz.ReactToaster

type ISliderProp =
    interface
    end

type ISliderStylesProp =
    interface
    end

type ISliderTrackProp =
    interface
    end

type IDotStyleProp =
    interface
    end
